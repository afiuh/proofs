# 人类大脑非图灵机论证（形式化重述）

> **版本**：2026-09-20
> **对应**：Lean 形式化 `lean/BrainNotTM.lean`（本仓库，零 sorry）
> **说明**：本版按形式化的结构重述论证。推理链已经 Lean 内核机器验证；论证的承诺收敛为 15 个明确点（7 原始概念 + 3 概念分析 + 5 实质前提）——全部明示，其余不可质疑。

---

## 〇、结构总览

形式化把论证分成四层：

```
给定定义（出发点——不可推）
    ↓ 推演（从给定定义导出概念）
实质前提（不可从定义推的断言）
    ↓ 推理（机器验证）
结论
```

**论证的全部承诺** = 7 原始概念 + 3 概念分析 + 5 实质前提 = **15 个明确点**。
除此之外的每一步（反证、归纳、二分、回溯链）都由 Lean 内核验证——给定承诺，结论必然。

---

## 一、给定定义（出发点）

### 1.1 原文的给定定义

- **算法**：有限、确定、可机械执行的步骤序列。
- **约束**：规则空间的边界。不是规则本身，是规则得以成立的框架。
- **约束生成**：创造规则空间边界的行为。
- **图灵机**：可执行任何算法的理想计算模型。
- **信息**：对外部状态的描述，可通过感觉器官输入大脑。
- **非算法过程**：不可由图灵机算法执行的过程。

### 1.2 形式化的原始概念（7 个——未定义的占位符）

| # | 概念 | 类型 | 意思 | 对应 |
|---|---|---|---|---|
| 1 | 过程 | 论域 | 执行者（含算法与非算法过程）| — |
| 2 | 约束空间 | 论域 | 规则空间的边界（什么算一步/如何连接/什么有效/何时停）| 约束 |
| 3 | 预设 | 关系 | `预设 p s`：p 的存在以 s 为条件 | 算法的步骤结构关系 |
| 4 | 生成 | 关系 | `生成 p s`：p 创造（给出）s | 约束生成 |
| 5 | 在颅内执行 | 谓词 | p 发生在大脑内部 | 前提 Q |
| 6 | 颅外输入 | 谓词 | p 从颅外（以输入形式）进入 | F2 |
| 7 | 是信息 | 谓词 | p 是描述而非执行 | 信息 |

**形式标志**：它们没有定义体（只声明、不定义）——这是"原始"的标志。

---

## 二、推演（从给定定义导出——有来源，不是给定）

### 第 1 步：预设（从"步骤序列"）

"算法 = 步骤序列"——"步骤序列"的含义里包含步骤结构（什么算一步、如何连接、什么算有效、何时停止）——即约束空间。

⇒ **预设**（`Presupposes`）= 过程与其步骤结构的关系。

### 第 2 步：确定 = 上游链可及（从前提 P 的含义）

"确定性是算法存在的前提……不能是'渐进确定'或'无限逼近的极限'"——无限回溯意味着确定性无处落地；"确定"即"有尽头"。

⇒ **确定**（`Determined s`）= 上游链可及（有限、有尽头）。

其中"上游"（`dep`）= 生成-预设步的传递闭包：a 是 b 的上游 ⟺ 存在一条链从 a 到 b，每一步形如"某过程预设前一环、生成后一环"。

### 第 3 步：算法 = 有确定步骤结构的过程（反证）

**反证**：假设约束空间不确定——则算法不可能确定（确定的执行需要确定的规则结构）；算法不确定则不存在（"确定"是算法的定义性要求）——与"算法存在"矛盾。故约束空间必须确定。

⇒ **算法**（`IsAlgorithm p`）= 有**确定的**步骤结构（预设空间）的过程。
⇒ "算法以确定的约束空间为存在条件"是**定义展开**，无需假设。

---

## 三、实质前提（5 条——不可从定义推）

### 前提 ①：世界上存在算法

至少存在一个算法。
（论证的起点——替代原版 F1"约束生成发生了"。由推演第 3 步展开，直接给出起点空间 s₀。）

### 前提 ②：确定的约束空间必由某过程生成

任何确定的约束空间都由某个过程生成——**不存在"自存"的确定空间**。
（对应原版第五步——"大脑中的算法"这一语境的锚定。）

### 前提 ③：来源二分穷尽

任何非算法过程，要么在颅内执行、要么是颅外输入——**来源穷尽于这两种**。
（论域：约束生成链涉及的过程——它们要么在颅内执行、要么从颅外以输入形式进入。**与大脑无关的颅外执行过程不在论域**——不进入本大脑的约束生成链。对应原版第八步 + 前提 Q。）

### 前提 ④：颅外只能输入信息

颅外能输入的东西**只能是信息**（描述）。
（对应原版 F2。）

### 前提 ⑤：非算法过程不是信息

非算法过程**不是信息**——它是执行，不是描述。
（对应原版第八步。）

---

## 四、推理（机器验证）

### 定理 1：存在非算法过程

**证明**（构造性——不依赖反证）：

1. 由前提①取一个算法 p₀；由定义展开，p₀ 有确定的约束空间 s₀（s₀ 的上游链可及）。
2. 对"可及"做归纳。设 x 是任意确定空间：
   - 由前提②，x 有生成者 p。
   - **情形 (a)**：p 是算法——由定义展开，p 有确定的约束空间 s'（s' 是 x 的上游）。
     递归到 s'（可及性保证递归良基）。
   - **情形 (b)**：p 不是算法——**找到非算法过程**。
3. 由 s₀ 的可及性，回溯链有限——**必到达情形 (b)**。

∎ 存在非算法过程。

**注**：这就是原版"递归链必须终止于非算法过程"的精确形式——终止机制是：算法生成者必然引出更上游的空间（链继续），非算法生成者不引出（链终止）；而"确定 = 可及"保证链不可能无限。

### 定理 2：非算法过程在大脑内

**证明**：

1. 取定理 1 给出的非算法过程 p。
2. 由前提③（二分）：p 要么在颅内、要么是颅外输入。
3. 若 p 是颅外输入——由前提④它是信息，由前提⑤非算法过程不是信息——**矛盾**。
4. 故 p 在颅内。

∎ 存在非算法过程，且在大脑内。

### 结论：大脑不是图灵机

- 大脑内部包含非算法过程（定理 2）。
- 图灵机 = 所有过程都是算法（给定定义：可执行任何算法的理想计算模型）。
- **所以：大脑不是图灵机。** ∎

---

## 五、与原文九步的对照

| 原文 | 形式化版 |
|---|---|
| 第一步（算法预设确定空间）| **推演第 3 步**（算法 = 有确定步骤结构——定义展开）|
| 第二步（约束生成的性质）| 概念澄清（不进入形式化）|
| 第三、四步（假设 + A₀ 预设）| **推理**（定理 1 的情形 (a)——定义展开）|
| 第五步（S₀ 的来源）| **前提②** |
| 第六、七步（递归链 + 终止）| **定理 1 的推理**（回溯链 + 可及性）|
| 第八步（在大脑内）| **定理 2**（前提③④⑤）|
| 第九步（结论）| **结论** |
| F1（约束生成发生了）| **前提①**（存在算法——更弱且更自然）|
| F2（颅外只输入信息）| **前提④⑤** |
| 前提 P（确定性是前提）| **推演第 2 步**（确定 = 可及）|
| 前提 Q（颅内执行）| **前提③**（二分框架）|

---

## 六、承诺清单（可质疑的全部 15 个点）

**原始概念（7）**：过程、约束空间、预设、生成、在颅内执行、颅外输入、是信息。
→ 质疑点：它们的**语义**（指什么）。

**概念分析（3）**：预设（步骤结构）；确定 = 可及；算法 = 有确定步骤结构。
→ 质疑点：**分析是否忠实**（"确定"真的等价于"可及"吗？）。

**实质前提（5）**：① 存在算法；② 确定空间必被生成；③ 二分穷尽；④ 颅外只输入信息；⑤ 非算法过程不是信息。
→ 质疑点：它们的**真**。

**其余一切**（推理链、定理、结论）——机器验证，给定承诺后不可质疑。

---

## 七、承诺的辩护（为什么这些难以被推翻）

### 定义层——全部有依据

| 承诺 | 为什么有效 | 攻击尝试 | 难度 |
|---|---|---|---|
| 原始概念（7 个）| 常识语义——过程、约束空间、预设、生成、颅内、颅外输入、信息 | 语义替换（需替换整个概念框架）| 难 |
| 算法 = 有确定步骤结构 | 从**公认定义**推（“算法 = 步骤序列”——“步骤序列”的含义包含步骤结构）| 改变“算法”的公认定义 | 难 |
| 上游 = 生成-预设链 | 术语**精确化**（“依赖链”的形式化——确定性传递的每一步结构）| 给出更好的精确化 | 难 |
| 确定 = 上游链可及 | 从**前提 P** 推——“确定性必须落地”：无限回溯意味着永远在等上游，没有任何环节“定下来”；“确定”的直觉就是“已经定下来” | “无限链也可以有确定性”（与“确定性=已定”的直觉冲突）| 难 |

### 实质前提——全部焊死或接近焊死

| 前提 | 为什么有效 | 攻击尝试 | 难度 |
|---|---|---|---|
| ① 存在算法 | **事实**——人类有算法（数学定义、程序、游戏规则），指认即可 | 整体怀疑论（超出论证范围）| **焊死** |
| ② 确定空间必被生成 | **语境**（大脑中的算法——约束空间是实现的、非抽象的）+ “实现必有实现者”（结构要被维持，得有东西维持它）| “空间可以自存”——在论域内等于说“神经结构没有神经基础”（自相矛盾）| **接近焊死** |
| ③ 论域内二分 | **定义性**——链上过程要么在颅内执行、要么从颅外以输入形式进入（第三类不在论域）| “界面过程”（论域限定已化解——界面过程要进入链条仍需通道）| **焊死** |
| ④ 颅外只输入信息 | **生理/经验**——感觉器官只传递信息；没有“直接输入执行过程”的机制（BCI 输入信号、镜像神经元是内部模拟、学习=重建）| 举出“直接输入执行过程”的机制（不存在）| **接近焊死** |
| ⑤ 非算法过程不是信息 | **范畴区分**——执行 vs 描述（层次不同）；无反例：程序文本是描述、执行是过程；DNA 序列是信息、转录是过程 | 举出“执行=描述”的东西（不存在）| **接近焊死** |

### 论域（Q：约束生成在颅内执行）——被颅外二分堵死

具身/延展认知的挑战：“认知过程不限于颅内——它是‘大脑-身体-环境’系统的。”

**回应（颅外二分）**：

- 身体/环境的过程若要参与链条——它们**在颅外**——**只能以输入形式**（信息）进入；不参与链条的——**不在论域**。
- **耦合不是第三类**：“大脑+身体+环境”的耦合 = “颅内步骤 + 输入步骤”的交替——每一步都落进二分。

**强延展认知要反驳**——必须举出一个“既非颅内、也非输入”的过程——举不出。

> 再怎么具身，人类算 1+1 的时候也只能用大脑里的神经细胞算，而不是脊髓里的神经细胞。

**难度：难**（二分堵死——挑战要成立，必须举出第三类过程，而它不存在）。

### 小结

**5 条前提：2 焊死 + 3 接近焊死；4 条定义：全部有依据；论域：被二分堵死**——**全部攻击面：焊死 / 接近焊死 / 难**。

---

## 八、已知的开放攻击面（诚实标注）

1. **前提③（二分穷尽）**——具身/延展认知的挑战已被论域二分吸收（见第七节）：任何“外部”过程要参与链条，只能以输入形式进入——不存在“既非颅内、也非输入”的第三类。**此项已从攻击面移除。**
2. **概念分析"确定 = 可及"**——若有人主张"确定"有更强/更弱的含义，可攻此分析。
3. **前提②（确定空间必被生成）**——在纯数学语境不成立（空间可"自存"）；论证的有效范围是"大脑中的算法"。

以上全部是**明牌**——攻击者只能正面对抗，不能暗算。

---

*形式化代码：[`lean/BrainNotTM.lean`](./lean/BrainNotTM.lean)*
*验证：用 Lean 4 编译（零 sorry）*
